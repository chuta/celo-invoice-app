# Netlify Redirects for SPA (Single Page Application)
# This ensures all routes are handled by React Router

# Redirect all routes to index.html for client-side routing
/*    /index.html   200

# API proxy (if needed in the future)
# /api/*  https://your-api-url.com/:splat  200

# Supabase Edge Functions proxy (optional)
# /functions/*  https://your-project.supabase.co/functions/v1/:splat  200
